/**
 * ค่าเวลาของห่วงโซ่คำสั่งอุปกรณ์ — แยกมาไว้ที่โมดูลกลาง (ไม่ผูกกับ React)
 * เพื่อให้ `FarmStateProvider` (เจ้าของ auto-cutoff ปั๊ม) กับ `useDeviceCommand`
 * ใช้ค่าชุดเดียวกันได้โดยไม่เกิด import วน (hook พวกนั้น import provider อยู่แล้ว)
 */

/** เวลารอผลตอบกลับจากอุปกรณ์จริง (จำลอง) — ต้นแบบใช้ 1.7 วินาที */
export const SEND_LATENCY_MS = 1700;

/** ปุ่มโชว์เครื่องหมายถูกหลังคำสั่งสำเร็จ */
export const DONE_FLASH_MS = 950;

/**
 * ปั๊มเปิดค้างได้นานสุดต่อเนื่องก่อนปิดอัตโนมัติ (safety) — 20 นาที
 * แทน guard G1 เดิม (ที่กันตามระดับน้ำใน mock) ด้วยตัวตัดอัตโนมัติ ไม่ให้ปั๊มเดินค้างลืมปิด
 * นี่เป็น safety cutoff ของระบบ **ไม่ใช่** ตัวเลือกให้ผู้ใช้ตั้ง "รดกี่นาที"
 *
 * เจ้าของตัวจับเวลาอยู่ที่ `FarmStateProvider` (ระดับแอป) ไม่ใช่ต่อหน้า —
 * ไม่งั้นออกจากหน้าควบคุมแล้ว timer ถูกล้าง ปั๊มเดินค้างเกิน 20 นาทีได้ (safety หลุด)
 */
export const PUMP_CUTOFF_MS = 20 * 60 * 1000;

/** control log เก็บไว้กี่รายการ — ที่เดียวของทั้งระบบ */
export const LOG_LIMIT = 30;

/**
 * โหมดจริง: หลัง cmd_result ok แล้วรอ `led{channel}` จริงยืนยันนานสุดเท่าไรก่อนปลด pending เอง
 *
 * วัดจริงจาก smoke-test: led เปลี่ยนตามหลังส่ง ~8-9.5 วินาที (device อ่านค่าทุก ~10 วิ)
 * ตั้ง 16 วิ = สูงกว่าที่วัดได้พอสมควร แต่ยังไม่ค้างถาวรถ้า led ไม่ยืนยัน (เช่น automation แบบ OR ทับ)
 */
export const LED_CONFIRM_TIMEOUT_MS = 16_000;

/**
 * อุปกรณ์เขียนสถานะจริง (shadow_ts) ทุก ~10 วิ · ถ้าเงียบเกินค่านี้ = ออฟไลน์ · led/เซนเซอร์ที่เห็นเป็น "ค่าค้าง"
 * ตั้ง 90 วิ = พลาดได้ ~9 รอบ กัน false alarm จาก network jitter แต่ยังจับออฟไลน์ได้ไว (ดู liveStatus/ConnectionPill)
 */
export const DEVICE_STALE_MS = 90_000;

/**
 * เซนเซอร์**รายตัว**เงียบเกินค่านี้ = ค่าที่เห็นเป็นค่าค้างของตัวนั้น (แม้บอร์ดยังมีชีวิต)
 *
 * 🔴 ต่างจาก `DEVICE_STALE_MS` คนละเรื่อง — ตัวนั้นดู `shadow_ts` ซึ่งเป็นชีพจรของ**ทั้งบอร์ด**
 * บอร์ดยังส่ง shadow_ts ปกติได้ทั้งที่เซนเซอร์ตัวหนึ่งถูกถอดออกไปแล้ว → ไม่มีอะไรฟ้องเลย
 * (เกิดจริง 2026-08-11: ทีมฮาร์ดแวร์ถอดเซนเซอร์ความชื้นดินออก ค่าค้างนิ่ง
 *  แต่หน้าจอยังติดป้าย "ค่าจริง" และ 8 แปลงทั้งฉากเกม/หน้าชลประทานยังคำนวณจากมัน
 *  รวมถึงรายการ "ต้องดูแลด่วน" ที่แนะนำให้ไปรดน้ำ — จากตัวเลขที่ไม่มีใครวัด)
 *
 * ตั้ง 5 นาที ไม่ใช่ 90 วิ เพราะ telemetry รายค่าอาจส่งถี่น้อยกว่า shadow_ts และสะดุดได้
 * เซนเซอร์ที่ถูกถอดออกจริงจะไม่กลับมาเอง ช้าอีก 3 นาทีจึงไม่เสียหาย แต่ false alarm เสียหายกว่า
 */
export const SENSOR_STALE_MS = 5 * 60 * 1000;

/**
 * ── ควบคุมความชื้นด้วยพัดลมดูดอากาศ (แอปสั่งอัตโนมัติตาม RH) ──
 * ค่าที่ออกแบบให้ "ประหยัดไฟ" — กันพัดลมเปิด-ปิดถี่ (short-cycle) ที่กินไฟ+สึกหรอมอเตอร์มากสุด
 * เจ้าของ engine อยู่ที่ `FarmStateProvider` (mount ครั้งเดียว) · แยกค่าไว้ที่นี่ให้เทส import ได้
 */
export const HUM_TICK_MS = 20_000; // ประเมินทุก 20 วิ (RH ไม่ต้องเช็คถี่กว่านี้)
export const HUM_MIN_RUN_MS = 10 * 60 * 1000; // เปิดแล้วต้องเดินอย่างน้อย 10 นาที (กันกระพริบ)
export const HUM_MIN_OFF_MS = 10 * 60 * 1000; // ปิดแล้วต้องพักอย่างน้อย 10 นาที
export const HUM_STAGE_DELAY_MS = 15 * 60 * 1000; // เปิดตัวแรกแล้ว 15 นาที RH ยังไม่ลง ค่อยเสริมตัวที่ 2
export const HUM_MAX_RUN_MS = 40 * 60 * 1000; // เปิดต่อเนื่องเกิน 40 นาที = ตัดพัก (ดูดไม่ลง แปลว่านอกก็ชื้น)
export const HUM_REST_MS = 15 * 60 * 1000; // หลังตัดเวลาสูงสุด พักห้ามเปิดซ้ำ 15 นาที
