# ให้ git เก็บทุกไฟล์ข้อความเป็น LF ใน repo เสมอ
# กันกรณีทำงานข้ามเครื่อง Windows/Mac แล้วได้ diff ทั้งไฟล์ทั้งที่แก้บรรทัดเดียว
* text=auto eol=lf

# ไฟล์ไบนารี — ห้าม git แตะไบต์เด็ดขาด
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
